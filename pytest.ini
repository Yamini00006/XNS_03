[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath =
    backend
    .
testpaths =
    tests

markers =
    integration: tests that verify integration between application components
    e2e: end-to-end application tests